import { NoNumbersInput } from './no-numbers-input';

describe('NoNumbersInput', () => {
  it('should create an instance', () => {
    const directive = new NoNumbersInput();
    expect(directive).toBeTruthy();
  });
});
